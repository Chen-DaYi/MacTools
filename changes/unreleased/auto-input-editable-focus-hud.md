---
release: plugin
type: fixed
area: Auto Input
---

Auto Input now shows its HUD in same-app fields, Google Docs, Raycast, and MacTools searches; it avoids input controls and adds a quieter mode that stays hidden until the input source changes.
