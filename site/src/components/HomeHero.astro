---
import { loadPluginCatalog, localizedPluginText, type Plugin } from "@/lib/plugins";

interface Props {
  downloadURL: string;
}

const { downloadURL } = Astro.props;
const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const withBase = (path: string) => `${base}${path.replace(/^\//, "")}`;

const installCommand = "brew install --cask mactools";
const sparkBars = Array.from({ length: 14 });
const catalog = await loadPluginCatalog();
const pluginsByID = new Map(catalog.plugins.map((plugin) => [plugin.id, plugin]));

type InteractionType = "disclosure" | "switch" | "button";
type LocalizedLabel = { zh: string; en: string };
type DetailListOption = LocalizedLabel & {
  id: string;
  mode: "action" | "switch" | "select" | "navigation";
  symbol?: string;
  subtitle?: LocalizedLabel;
  selected?: boolean;
  enabled?: boolean;
  divider?: boolean;
};
type DetailModel = {
  label?: LocalizedLabel;
  minimum?: number;
  maximum?: number;
  value?: number;
  unit?: string;
  options?: DetailListOption[];
};

const interactionGroups: Array<{ type: InteractionType; ids: string[] }> = [
  {
    type: "disclosure",
    ids: [
      "display-brightness",
      "display-resolution",
      "sidecar",
      "disk-clean",
      "xcode-clean",
      "battery-charge-limit",
      "fan-control",
      "activity-bar",
      "launch-control",
    ],
  },
  {
    type: "switch",
    ids: [
      "menu-bar-hidden",
      "appearance",
      "auto-hide-menu-bar",
      "auto-hide-dock",
      "display-true-color",
      "night-shift",
      "hide-notch",
      "stage-manager",
      "keep-awake",
      "system-mute",
      "microphone-mute",
      "app-hotkey",
      "translator",
      "physical-clean-mode",
    ],
  },
  {
    type: "button",
    ids: [
      "launchpad",
      "window-switcher",
      "right-click",
      "ip-overview",
      "display-sleep",
      "lock-screen",
      "eject-disk",
      "quit-apps",
      "empty-trash",
      "clipboard-clear",
      "fix-damaged-app",
      "mouse-enhancer",
      "homebrew",
      "zsh-config",
    ],
  },
];

const pluginSymbols: Record<string, string> = {
  "activity-bar": "chart.bar.xaxis",
  "app-hotkey": "keyboard",
  "appearance": "circle.lefthalf.filled",
  "auto-hide-dock": "rectangle.bottomthird.inset.filled",
  "auto-hide-menu-bar": "menubar.rectangle",
  "battery-charge-limit": "battery.100.bolt",
  "clipboard-clear": "trash",
  "disk-clean": "internaldrive",
  "display-brightness": "sun.max",
  "display-resolution": "display",
  "display-sleep": "display",
  "display-true-color": "circle.righthalf.filled",
  "eject-disk": "eject",
  "empty-trash": "trash",
  "fan-control": "fan",
  "fix-damaged-app": "wrench.and.screwdriver.fill",
  "hide-notch": "rectangle.topthird.inset.filled",
  "homebrew": "shippingbox.fill",
  "ip-overview": "network",
  "keep-awake": "moon",
  "launch-control": "powerplug",
  "launchpad": "square.grid.3x3.fill",
  "lock-screen": "lock",
  "menu-bar-hidden": "menubar.arrow.up.rectangle",
  "microphone-mute": "mic.slash",
  "mouse-enhancer": "computermouse",
  "night-shift": "lamp.floor",
  "physical-clean-mode": "sparkles",
  "quit-apps": "power",
  "right-click": "contextualmenu.and.cursorarrow",
  "sidecar": "display",
  "stage-manager": "sidebar.squares.leading",
  "system-mute": "speaker.slash",
  "translator": "text.bubble",
  "window-switcher": "rectangle.2.swap",
  "xcode-clean": "hammer",
  "zsh-config": "curlybraces",
};

const symbolStyle = (name: string) => `--sim-symbol: url("${withBase(`/assets/sf-symbols/${name}.png`)}")`;

const actionLabels: Record<string, LocalizedLabel> = {
  "clipboard-clear": { zh: "清空", en: "Clear" },
  "display-sleep": { zh: "休眠", en: "Sleep" },
  "eject-disk": { zh: "推出", en: "Eject" },
  "empty-trash": { zh: "清空", en: "Empty" },
  "fix-damaged-app": { zh: "选择", en: "Choose" },
  "homebrew": { zh: "管理", en: "Manage" },
  "ip-overview": { zh: "检测", en: "Check" },
  "launchpad": { zh: "打开", en: "Open" },
  "lock-screen": { zh: "锁定", en: "Lock" },
  "mouse-enhancer": { zh: "设置", en: "Settings" },
  "quit-apps": { zh: "选择", en: "Choose" },
  "right-click": { zh: "设置", en: "Settings" },
  "window-switcher": { zh: "设置", en: "Settings" },
  "zsh-config": { zh: "编辑", en: "Edit" },
};

const disclosureDetails: Record<string, DetailModel> = {
  "display-brightness": {
    label: { zh: "内建显示器", en: "Built-in display" },
    minimum: 10,
    maximum: 100,
    value: 78,
    unit: "%",
  },
  "display-resolution": {
    options: [
      {
        id: "builtin-display",
        mode: "navigation",
        zh: "内建显示器",
        en: "Built-in Display",
        subtitle: { zh: "2560 × 1440", en: "2560 × 1440" },
      },
      {
        id: "display-settings",
        mode: "action",
        symbol: "display",
        zh: "打开系统显示器设置",
        en: "Open Display Settings",
        divider: true,
      },
    ],
  },
  "sidecar": {
    label: { zh: "可用的 Sidecar 显示器", en: "Available Sidecar Displays" },
    options: [
      { id: "ipad", mode: "action", symbol: "circle", zh: "iPad Pro · 连接", en: "iPad Pro · Connect" },
    ],
  },
  "disk-clean": {
    options: [
      { id: "scan", mode: "action", symbol: "magnifyingglass", zh: "扫描", en: "Scan" },
      { id: "clean", mode: "action", symbol: "trash", zh: "清理", en: "Clean" },
      { id: "details", mode: "action", symbol: "arrow.up.right.square", zh: "打开清理详情", en: "Open Cleanup Details", divider: true },
    ],
  },
  "xcode-clean": {
    options: [
      { id: "scan", mode: "action", symbol: "magnifyingglass", zh: "扫描 Xcode 数据", en: "Scan Xcode Data" },
      { id: "clean", mode: "action", symbol: "trash", zh: "清理已选项目", en: "Clean Selected Items" },
      { id: "details", mode: "action", symbol: "arrow.up.right.square", zh: "打开清理详情", en: "Open Cleanup Details", divider: true },
    ],
  },
  "battery-charge-limit": {
    label: { zh: "充电上限", en: "Charge limit" },
    minimum: 50,
    maximum: 100,
    value: 80,
    unit: "%",
    options: [
      { id: "disable", mode: "action", symbol: "checkmark.circle.fill", zh: "停用充电上限", en: "Disable Charge Limit" },
      { id: "charge", mode: "action", symbol: "bolt.fill", zh: "开始充电", en: "Start Charging", divider: true },
      { id: "settings", mode: "action", symbol: "slider.horizontal.3", zh: "设置…", en: "Settings…", divider: true },
    ],
  },
  "fan-control": {
    options: [
      { id: "auto", mode: "select", zh: "自动", en: "Automatic", selected: true },
      { id: "quiet", mode: "select", zh: "静音", en: "Quiet" },
      { id: "full", mode: "select", zh: "全速", en: "Full Speed" },
      { id: "manage", mode: "action", symbol: "slider.horizontal.3", zh: "管理预设…", en: "Manage Presets…", divider: true },
    ],
  },
  "activity-bar": {
    options: [
      { id: "tracking", mode: "switch", symbol: "chart.bar.xaxis", zh: "活动统计", en: "Activity Tracking", selected: true },
      { id: "input", mode: "action", symbol: "keyboard.badge.eye", zh: "打开输入监控设置", en: "Open Input Monitoring Settings" },
      { id: "hooks", mode: "action", symbol: "terminal", zh: "安装 AI 编程 Hook", en: "Install AI Coding Hooks" },
      { id: "reset", mode: "action", symbol: "arrow.counterclockwise", zh: "清空今日统计", en: "Clear Today's Stats", divider: true },
    ],
  },
  "launch-control": {
    options: [
      { id: "refresh", mode: "action", symbol: "arrow.clockwise", zh: "刷新列表", en: "Refresh List" },
      { id: "manager", mode: "action", symbol: "arrow.up.right.square", zh: "打开管理器", en: "Open Manager", divider: true },
    ],
  },
};

const simulatedDisplayResolutions = [
  { id: "5120x2880", zh: "5120×2880 (LoDPI)", en: "5120×2880 (LoDPI)" },
  { id: "4096x2304", zh: "4096×2304 (LoDPI)", en: "4096×2304 (LoDPI)" },
  { id: "3200x1800", zh: "3200×1800 (HiDPI)", en: "3200×1800 (HiDPI)" },
  { id: "3008x1692", zh: "3008×1692 (原生)", en: "3008×1692 (Native)" },
  { id: "2560x1440", zh: "2560×1440 (默认)", en: "2560×1440 (Default)", selected: true },
  { id: "2048x1152", zh: "2048×1152 (HiDPI)", en: "2048×1152 (HiDPI)" },
  { id: "1920x1080", zh: "1920×1080 (HiDPI)", en: "1920×1080 (HiDPI)" },
];

const defaultEnabled = new Set([
  "menu-bar-hidden",
  "appearance",
  "display-true-color",
  "keep-awake",
  "app-hotkey",
]);

const panelEntries = interactionGroups.flatMap(({ type, ids }) =>
  ids.flatMap((id) => {
    const plugin = pluginsByID.get(id);
    if (!plugin) return [];
    return [{ plugin, text: localizedPluginText(plugin), type }];
  })
);

const dashboardIDs = ["system-status", "device-battery", "activity-bar", "calendar"] as const;
const dashboardPlugins = Object.fromEntries(
  dashboardIDs.flatMap((id) => {
    const plugin = pluginsByID.get(id);
    return plugin ? [[id, { plugin, text: localizedPluginText(plugin) }]] : [];
  })
) as Record<string, { plugin: Plugin; text: ReturnType<typeof localizedPluginText> }>;

const now = new Date();
const calendarYear = now.getFullYear();
const calendarMonth = now.getMonth();
const mondayFirstOffset = (new Date(calendarYear, calendarMonth, 1).getDay() + 6) % 7;
const lunarDayLabels = [
  "初一", "初二", "初三", "初四", "初五", "初六", "初七", "初八", "初九", "初十",
  "十一", "十二", "十三", "十四", "十五", "十六", "十七", "十八", "十九", "二十",
  "廿一", "廿二", "廿三", "廿四", "廿五", "廿六", "廿七", "廿八", "廿九", "三十",
];
const calendarCells = Array.from({ length: 42 }, (_, index) => {
  const date = new Date(calendarYear, calendarMonth, index - mondayFirstOffset + 1);
  const day = date.getDate();
  const isCurrentMonth = date.getMonth() === calendarMonth;
  const isToday = date.toDateString() === now.toDateString();
  return {
    date,
    day,
    isCurrentMonth,
    isToday,
    lunarLabel: lunarDayLabels[(day + 8) % lunarDayLabels.length],
    dayCode: `D${((day + 14) % 30) + 1}`,
  };
});
const calendarTitleZh = new Intl.DateTimeFormat("zh-CN", { year: "numeric", month: "long" }).format(now);
const calendarTitleEn = new Intl.DateTimeFormat("en-US", { year: "numeric", month: "long" }).format(now);
---

<section class="home-hero" aria-labelledby="home-title">
  <div class="home-hero-copy">
    <div class="hero-lockup">
      <img src={withBase("/assets/logo-mactools-rounded.png")} width="72" height="72" alt="" />
      <p class="eyebrow">
        <span data-i18n-zh>原生 macOS 菜单栏工具集</span>
        <span data-i18n-en>Native macOS menu bar toolkit</span>
      </p>
    </div>
    <h1 id="home-title">
      <span class="hero-title-zh" data-i18n-zh>
        <span>常用系统操作，</span>
        <span>安静地留在菜单栏。</span>
      </span>
      <span data-i18n-en>Everyday system controls, kept quietly in the menu bar.</span>
    </h1>
    <p class="hero-lead">
      <span data-i18n-zh>MacTools 把显示器、清理、休眠、启动台、插件市场和更多高频动作集中到一个轻量入口。</span>
      <span data-i18n-en>MacTools collects display controls, cleanup, sleep, Launchpad, plugins, and other frequent actions in one lightweight native app.</span>
    </p>
    <div class="hero-actions">
      <a class="button primary" href={downloadURL} target="_blank" rel="noopener noreferrer">
        <span data-i18n-zh>立即下载</span>
        <span data-i18n-en>Download MacTools</span>
      </a>
      <a class="button subtle" href="https://github.com/ggbond268/MacTools/releases" target="_blank" rel="noopener noreferrer">
        <span data-i18n-zh>更新记录</span>
        <span data-i18n-en>What's new</span>
      </a>
    </div>
    <div class="homebrew-panel" id="install">
      <p>
        <span data-i18n-zh>也可以使用 Homebrew 安装</span>
        <span data-i18n-en>Or install with Homebrew</span>
      </p>
      <div class="command-line">
        <span>$</span>
        <code>{installCommand}</code>
        <button type="button" data-copy={installCommand}>
          <span data-i18n-zh>复制</span>
          <span data-i18n-en>Copy</span>
        </button>
      </div>
    </div>
  </div>

  <div class="hero-product">
    <div class="menu-panel-showcase" data-menubar-simulator data-preview-theme="light">
      <section
        class="sim-panel"
        id="hero-mactools-panel"
        aria-label="MacTools 模拟面板"
        data-aria-zh="MacTools 模拟面板"
        data-aria-en="MacTools simulated panel"
      >
        <div class="sim-panel-notch" aria-hidden="true"></div>
        <div class="sim-panel-toolbar">
          <div class="sim-panel-tabs" role="tablist">
            <button
              class="sim-panel-tab is-selected"
              type="button"
              role="tab"
              id="sim-monitor-tab"
              aria-controls="sim-monitor-panel"
              aria-selected="true"
              aria-label="组件面板"
              data-aria-zh="组件面板"
              data-aria-en="Components"
              data-sim-tab="monitor"
            >
              <span class="sim-sf-symbol sim-tab-symbol" style={symbolStyle("square.grid.2x2")} aria-hidden="true"></span>
              <span class="sim-tooltip" aria-hidden="true">
                <span data-i18n-zh>组件面板</span>
                <span data-i18n-en>Components</span>
              </span>
            </button>
            <button
              class="sim-panel-tab"
              type="button"
              role="tab"
              id="sim-controls-tab"
              aria-controls="sim-controls-panel"
              aria-selected="false"
              aria-label="功能面板"
              data-aria-zh="功能面板"
              data-aria-en="Controls"
              data-sim-tab="controls"
            >
              <span class="sim-sf-symbol sim-tab-symbol" style={symbolStyle("switch.2")} aria-hidden="true"></span>
              <span class="sim-tooltip" aria-hidden="true">
                <span data-i18n-zh>功能面板</span>
                <span data-i18n-en>Controls</span>
              </span>
            </button>
          </div>
        </div>

        <div
          class="sim-panel-view sim-monitor-view is-visible"
          id="sim-monitor-panel"
          role="tabpanel"
          aria-labelledby="sim-monitor-tab"
          data-sim-view="monitor"
        >
          <div class="sim-dashboard-stack">
            <article
              class="sim-dashboard-widget sim-system-widget"
              aria-label={dashboardPlugins["system-status"]?.text.zh.displayName ?? "系统状态"}
              data-aria-zh={dashboardPlugins["system-status"]?.text.zh.displayName ?? "系统状态"}
              data-aria-en={dashboardPlugins["system-status"]?.text.en.displayName ?? "System Status"}
              title={dashboardPlugins["system-status"]?.text.zh.summary}
              data-title-zh={dashboardPlugins["system-status"]?.text.zh.summary}
              data-title-en={dashboardPlugins["system-status"]?.text.en.summary}
            >
              <div class="sim-system-grid">
                <section class="sim-hud-tile sim-hud-cpu" data-sim-metric="cpu">
                  <header><strong><i class="sim-sf-symbol" style={symbolStyle("cpu")} aria-hidden="true"></i>CPU</strong><span class="sim-hud-chip" data-sim-temp>51°C</span></header>
                  <div class="sim-hud-reading"><b data-sim-value>24</b><small>%</small></div>
                  <div class="sim-spark-bars" aria-hidden="true">{sparkBars.map(() => <i></i>)}</div>
                  <footer><span data-sim-footnote><span data-i18n-zh>负载 1.42 · 功率 2.8 W</span><span data-i18n-en>Load 1.42 · Power 2.8 W</span></span></footer>
                </section>

                <section class="sim-hud-tile sim-hud-gpu" data-sim-metric="gpu">
                  <header><strong><i class="sim-sf-symbol" style={symbolStyle("cpu.fill")} aria-hidden="true"></i>GPU</strong><span class="sim-hud-chip" data-sim-temp>47°C</span></header>
                  <div class="sim-hud-reading"><b data-sim-value>18</b><small>%</small></div>
                  <div class="sim-spark-bars" aria-hidden="true">{sparkBars.map(() => <i></i>)}</div>
                  <footer><span data-sim-footnote>M1 Max</span></footer>
                </section>

                <section class="sim-hud-tile sim-hud-network" data-sim-metric="network">
                  <header><strong><i class="sim-sf-symbol" style={symbolStyle("network")} aria-hidden="true"></i><span data-i18n-zh>网络</span><span data-i18n-en>Network</span></strong><span class="sim-hud-chip"><span data-i18n-zh>有线</span><span data-i18n-en>Wired</span></span></header>
                  <div class="sim-hud-reading"><b data-sim-value>4.8</b><small>MB/s</small></div>
                  <svg class="sim-hud-chart" viewBox="0 0 120 22" preserveAspectRatio="none" aria-hidden="true">
                    <path class="sim-chart-area sim-chart-download" data-sim-chart-area="network-download" d="M0 22 L0 17 L10 14 L20 16 L30 9 L40 12 L50 5 L60 11 L70 8 L80 13 L90 4 L100 7 L110 3 L120 8 L120 22 Z"></path>
                    <path class="sim-chart-line sim-chart-download" data-sim-chart-line="network-download" d="M0 17 L10 14 L20 16 L30 9 L40 12 L50 5 L60 11 L70 8 L80 13 L90 4 L100 7 L110 3 L120 8"></path>
                    <path class="sim-chart-area sim-chart-upload" data-sim-chart-area="network-upload" d="M0 22 L0 19 L10 18 L20 20 L30 15 L40 17 L50 12 L60 16 L70 14 L80 18 L90 11 L100 16 L110 13 L120 17 L120 22 Z"></path>
                    <path class="sim-chart-line sim-chart-upload" data-sim-chart-line="network-upload" d="M0 19 L10 18 L20 20 L30 15 L40 17 L50 12 L60 16 L70 14 L80 18 L90 11 L100 16 L110 13 L120 17"></path>
                  </svg>
                  <footer class="sim-rate-footer"><span class="sim-download">↓ <b data-sim-down>3.7 MB/s</b></span><span class="sim-upload">↑ <b data-sim-up>1.1 MB/s</b></span></footer>
                </section>

                <section class="sim-hud-tile sim-hud-disk" data-sim-metric="disk">
                  <header><strong><i class="sim-sf-symbol" style={symbolStyle("internaldrive")} aria-hidden="true"></i><span data-i18n-zh>磁盘</span><span data-i18n-en>Disk</span></strong><span class="sim-hud-chip">228 GB</span></header>
                  <div class="sim-hud-reading"><b data-sim-value>41.0</b><small>GB <span data-i18n-zh>可用</span><span data-i18n-en>free</span></small></div>
                  <svg class="sim-hud-chart" viewBox="0 0 120 22" preserveAspectRatio="none" aria-hidden="true">
                    <path class="sim-chart-area sim-chart-disk-read" data-sim-chart-area="disk-read" d="M0 22 L0 18 L10 15 L20 19 L30 8 L40 12 L50 7 L60 16 L70 10 L80 13 L90 5 L100 11 L110 7 L120 14 L120 22 Z"></path>
                    <path class="sim-chart-line sim-chart-disk-read" data-sim-chart-line="disk-read" d="M0 18 L10 15 L20 19 L30 8 L40 12 L50 7 L60 16 L70 10 L80 13 L90 5 L100 11 L110 7 L120 14"></path>
                    <path class="sim-chart-area sim-chart-disk-write" data-sim-chart-area="disk-write" d="M0 22 L0 20 L10 17 L20 20 L30 15 L40 19 L50 13 L60 18 L70 15 L80 19 L90 12 L100 18 L110 14 L120 19 L120 22 Z"></path>
                    <path class="sim-chart-line sim-chart-disk-write" data-sim-chart-line="disk-write" d="M0 20 L10 17 L20 20 L30 15 L40 19 L50 13 L60 18 L70 15 L80 19 L90 12 L100 18 L110 14 L120 19"></path>
                  </svg>
                  <footer class="sim-rate-footer"><span class="sim-disk-read">R <b data-sim-disk-read>18 MB/s</b></span><span class="sim-disk-write">W <b data-sim-disk-write>5.2 MB/s</b></span></footer>
                </section>

                <section class="sim-hud-tile sim-hud-memory" data-sim-metric="memory">
                  <header><strong><i class="sim-sf-symbol" style={symbolStyle("memorychip")} aria-hidden="true"></i><span data-i18n-zh>内存</span><span data-i18n-en>Memory</span></strong><span class="sim-hud-chip">16 GB</span></header>
                  <div class="sim-hud-reading"><b data-sim-value>72</b><small>%</small></div>
                  <svg class="sim-hud-chart" viewBox="0 0 120 22" preserveAspectRatio="none" aria-hidden="true">
                    <path class="sim-chart-area sim-chart-memory" data-sim-chart-area="memory" d="M0 22 L0 15 L10 14 L20 13 L30 14 L40 11 L50 12 L60 9 L70 10 L80 7 L90 8 L100 6 L110 7 L120 5 L120 22 Z"></path>
                    <path class="sim-chart-line sim-chart-memory" data-sim-chart-line="memory" d="M0 15 L10 14 L20 13 L30 14 L40 11 L50 12 L60 9 L70 10 L80 7 L90 8 L100 6 L110 7 L120 5"></path>
                  </svg>
                  <footer><span data-sim-footnote><span data-i18n-zh>已用 <b data-sim-used>11.5</b> GB · 交换 320 MB</span><span data-i18n-en>Used <b data-sim-used>11.5</b> GB · Swap 320 MB</span></span></footer>
                </section>

                <section class="sim-hud-tile sim-hud-battery" data-sim-metric="battery">
                  <header><strong><i class="sim-sf-symbol" style={symbolStyle("battery.100")} aria-hidden="true"></i><span data-i18n-zh>电池</span><span data-i18n-en>Battery</span></strong><span class="sim-hud-chip">32°C</span></header>
                  <div class="sim-hud-reading"><b data-sim-value>84</b><small>%</small></div>
                  <svg class="sim-hud-chart" viewBox="0 0 120 22" preserveAspectRatio="none" aria-hidden="true">
                    <path class="sim-chart-area sim-chart-battery" data-sim-chart-area="battery" d="M0 22 L0 5 L10 6 L20 6 L30 7 L40 8 L50 8 L60 9 L70 10 L80 11 L90 12 L100 13 L110 14 L120 15 L120 22 Z"></path>
                    <path class="sim-chart-line sim-chart-battery" data-sim-chart-line="battery" d="M0 5 L10 6 L20 6 L30 7 L40 8 L50 8 L60 9 L70 10 L80 11 L90 12 L100 13 L110 14 L120 15"></path>
                  </svg>
                  <footer><span data-sim-footnote><span data-i18n-zh>健康 96% · 182 次循环</span><span data-i18n-en>96% health · 182 cycles</span></span></footer>
                </section>
              </div>

              <section class="sim-process-card">
                <header><strong><span data-i18n-zh>进程</span><span data-i18n-en>Processes</span></strong><span>CPU&nbsp;&nbsp;&nbsp;MEM</span></header>
                <div class="sim-process-list">
                  <div><span><i class="sim-process-dot process-blue"></i>MacTools</span><span><b data-sim-process-cpu="0">7.2%</b><b>1.1%</b></span></div>
                  <div><span><i class="sim-process-dot process-pink"></i>WindowServer</span><span><b data-sim-process-cpu="1">5.8%</b><b>3.4%</b></span></div>
                  <div><span><i class="sim-process-dot process-green"></i>Safari</span><span><b data-sim-process-cpu="2">3.1%</b><b>4.8%</b></span></div>
                </div>
              </section>
            </article>

            <article
              class="sim-dashboard-widget sim-device-widget"
              aria-label={dashboardPlugins["device-battery"]?.text.zh.displayName ?? "设备电量"}
              data-aria-zh={dashboardPlugins["device-battery"]?.text.zh.displayName ?? "设备电量"}
              data-aria-en={dashboardPlugins["device-battery"]?.text.en.displayName ?? "Device Battery"}
              title={dashboardPlugins["device-battery"]?.text.zh.summary}
              data-title-zh={dashboardPlugins["device-battery"]?.text.zh.summary}
              data-title-en={dashboardPlugins["device-battery"]?.text.en.summary}
            >
              <div class="sim-device-row">
                <span class="sim-device-icon" aria-hidden="true">
                  <i class="sim-sf-symbol" style={symbolStyle("laptopcomputer")}></i>
                </span>
                <strong>MacBook Pro</strong>
                <b data-sim-device-level="mac">84%</b>
                <span class="sim-device-battery"><i data-sim-device-bar="mac"></i></span>
              </div>
              <div class="sim-device-row">
                <span class="sim-device-icon" aria-hidden="true">
                  <i class="sim-sf-symbol" style={symbolStyle("iphone")}></i>
                </span>
                <strong>iPhone</strong>
                <b>29%</b>
                <span class="sim-device-battery"><i class="is-low" style="width: 29%"></i></span>
              </div>
              <div class="sim-device-row">
                <span class="sim-device-icon" aria-hidden="true">
                  <i class="sim-sf-symbol" style={symbolStyle("airpods.chargingcase")}></i>
                </span>
                <strong><span data-i18n-zh>AirPods 充电盒</span><span data-i18n-en>AirPods Case</span></strong>
                <b>87%</b>
                <span class="sim-device-battery"><i style="width: 87%"></i></span>
              </div>
              <div class="sim-device-row">
                <span class="sim-device-icon" aria-hidden="true">
                  <i class="sim-sf-symbol" style={symbolStyle("airpod.right")}></i>
                </span>
                <strong><span data-i18n-zh>右耳 AirPod</span><span data-i18n-en>Right AirPod</span></strong>
                <b>100%</b>
                <span class="sim-device-battery"><i style="width: 100%"></i></span>
              </div>
              <div class="sim-device-row">
                <span class="sim-device-icon" aria-hidden="true">
                  <i class="sim-sf-symbol" style={symbolStyle("airpod.left")}></i>
                </span>
                <strong><span data-i18n-zh>左耳 AirPod</span><span data-i18n-en>Left AirPod</span></strong>
                <b>100%</b>
                <span class="sim-device-battery"><i style="width: 100%"></i></span>
              </div>
              <div class="sim-device-row">
                <span class="sim-device-icon" aria-hidden="true">
                  <i class="sim-sf-symbol" style={symbolStyle("computermouse.fill")}></i>
                </span>
                <strong>MX Anywhere 3S</strong>
                <b>55%</b>
                <span class="sim-device-battery"><i style="width: 55%"></i></span>
              </div>
            </article>

            <article
              class="sim-dashboard-widget sim-activity-widget"
              aria-label={dashboardPlugins["activity-bar"]?.text.zh.displayName ?? "活动统计"}
              data-aria-zh={dashboardPlugins["activity-bar"]?.text.zh.displayName ?? "活动统计"}
              data-aria-en={dashboardPlugins["activity-bar"]?.text.en.displayName ?? "Activity Stats"}
              title={dashboardPlugins["activity-bar"]?.text.zh.summary}
              data-title-zh={dashboardPlugins["activity-bar"]?.text.zh.summary}
              data-title-en={dashboardPlugins["activity-bar"]?.text.en.summary}
            >
              <header class="sim-activity-header">
                <strong>
                  <span data-i18n-zh>{dashboardPlugins["activity-bar"]?.text.zh.displayName ?? "活动统计"}</span>
                  <span data-i18n-en>{dashboardPlugins["activity-bar"]?.text.en.displayName ?? "Activity Stats"}</span>
                </strong>
                <div>
                  <button
                    type="button"
                    data-sim-activity-day="-1"
                    aria-label="前一天"
                    data-aria-zh="前一天"
                    data-aria-en="Previous day"
                  ><span class="sim-sf-symbol" style={symbolStyle("chevron.left")} aria-hidden="true"></span></button>
                  <span data-sim-activity-date><span data-i18n-zh>今天</span><span data-i18n-en>Today</span></span>
                  <button
                    type="button"
                    data-sim-activity-day="1"
                    aria-label="后一天"
                    data-aria-zh="后一天"
                    data-aria-en="Next day"
                  ><span class="sim-sf-symbol" style={symbolStyle("chevron.right")} aria-hidden="true"></span></button>
                </div>
              </header>
              <div class="sim-activity-stats">
                <div><i class="sim-sf-symbol activity-blue" style={symbolStyle("keyboard")} aria-hidden="true"></i><span><b data-sim-activity-keys>8,426</b><small><span data-i18n-zh>按键</span><span data-i18n-en>Keystrokes</span></small></span></div>
                <div><i class="sim-sf-symbol activity-pink" style={symbolStyle("cursorarrow.click.2")} aria-hidden="true"></i><span><b data-sim-activity-clicks>2,184</b><small><span data-i18n-zh>点击</span><span data-i18n-en>Clicks</span></small></span></div>
                <div><i class="sim-sf-symbol activity-green" style={symbolStyle("scroll")} aria-hidden="true"></i><span><b data-sim-activity-scrolls>1,079</b><small><span data-i18n-zh>滚动</span><span data-i18n-en>Scrolls</span></small></span></div>
                <div><i class="sim-sf-symbol activity-orange" style={symbolStyle("macwindow.on.rectangle")} aria-hidden="true"></i><span><b data-sim-activity-time>4h 36m</b><small><span data-i18n-zh>屏幕时间</span><span data-i18n-en>Screen Time</span></small></span></div>
              </div>
              <div class="sim-activity-chart" aria-hidden="true">
                {sparkBars.map((_, index) => <i style={`height: ${28 + ((index * 23) % 62)}%`}></i>)}
              </div>
            </article>

            <article
              class="sim-dashboard-widget sim-calendar-widget"
              aria-label={dashboardPlugins["calendar"]?.text.zh.displayName ?? "日历"}
              data-aria-zh={dashboardPlugins["calendar"]?.text.zh.displayName ?? "日历"}
              data-aria-en={dashboardPlugins["calendar"]?.text.en.displayName ?? "Calendar"}
              title={dashboardPlugins["calendar"]?.text.zh.summary}
              data-title-zh={dashboardPlugins["calendar"]?.text.zh.summary}
              data-title-en={dashboardPlugins["calendar"]?.text.en.summary}
            >
              <header class="sim-calendar-header">
                <strong>
                  <span data-i18n-zh data-sim-calendar-title-zh>{calendarTitleZh}</span>
                  <span data-i18n-en data-sim-calendar-title-en>{calendarTitleEn}</span>
                </strong>
                <div>
                  <button
                    type="button"
                    data-sim-calendar-nav="previous"
                    aria-label="上个月"
                    data-aria-zh="上个月"
                    data-aria-en="Previous month"
                  ><span class="sim-sf-symbol" style={symbolStyle("chevron.left")} aria-hidden="true"></span></button>
                  <button type="button" data-sim-calendar-nav="today"><span data-i18n-zh>今天</span><span data-i18n-en>Today</span></button>
                  <button
                    type="button"
                    data-sim-calendar-nav="next"
                    aria-label="下个月"
                    data-aria-zh="下个月"
                    data-aria-en="Next month"
                  ><span class="sim-sf-symbol" style={symbolStyle("chevron.right")} aria-hidden="true"></span></button>
                </div>
              </header>
              <div class="sim-calendar-weekdays" aria-hidden="true">
                {[
                  { zh: "一", en: "M" },
                  { zh: "二", en: "T" },
                  { zh: "三", en: "W" },
                  { zh: "四", en: "T" },
                  { zh: "五", en: "F" },
                  { zh: "六", en: "S" },
                  { zh: "日", en: "S" },
                ].map((weekday) => (
                  <span><i data-i18n-zh>{weekday.zh}</i><i data-i18n-en>{weekday.en}</i></span>
                ))}
              </div>
              <div class="sim-calendar-grid">
                {calendarCells.map((cell) => (
                  <button
                    type="button"
                    class:list={{
                      "is-outside": !cell.isCurrentMonth,
                      "is-today": cell.isToday,
                      "has-events": cell.isCurrentMonth && [1, 8, 12, 18, 24].includes(cell.day),
                    }}
                    data-sim-calendar-cell
                    data-sim-calendar-day={cell.day}
                    aria-label={`${cell.date.getFullYear()}-${cell.date.getMonth() + 1}-${cell.day}`}
                  >
                    <b>{cell.day}</b>
                    <small>
                      <span data-i18n-zh>{cell.isToday ? "今天" : cell.lunarLabel}</span>
                      <span data-i18n-en>{cell.isToday ? "Today" : cell.dayCode}</span>
                    </small>
                    <span class="sim-calendar-event-dots" aria-hidden="true"></span>
                  </button>
                ))}
              </div>
            </article>
          </div>
        </div>

        <div
          class="sim-panel-view sim-controls-view"
          id="sim-controls-panel"
          role="tabpanel"
          aria-labelledby="sim-controls-tab"
          hidden
          data-sim-view="controls"
        >
          <div class="sim-feature-list">
            {panelEntries.map(({ plugin, text, type }) => {
              const symbol = pluginSymbols[plugin.id] ?? "app.dashed";
              const detail = disclosureDetails[plugin.id];
              const actionLabel = actionLabels[plugin.id] ?? { zh: "操作", en: "Run" };
              const isEnabled = defaultEnabled.has(plugin.id);
              const rowContent = (
                <>
                  <span class="sim-feature-icon" aria-hidden="true">
                    <i class="sim-sf-symbol" style={symbolStyle(symbol)}></i>
                  </span>
                  <span class="sim-feature-copy">
                    <strong><span data-i18n-zh>{text.zh.displayName}</span><span data-i18n-en>{text.en.displayName}</span></strong>
                    <small><span data-i18n-zh>{text.zh.summary}</span><span data-i18n-en>{text.en.summary}</span></small>
                  </span>
                </>
              );

              return (
                <article class={`sim-feature-item sim-feature-${type}`} data-sim-plugin={plugin.id}>
                  {type === "disclosure" ? (
                    <>
                      <button
                        class="sim-feature-row"
                        type="button"
                        data-sim-disclosure
                        aria-expanded="false"
                        aria-label={text.zh.displayName}
                        data-aria-zh={text.zh.displayName}
                        data-aria-en={text.en.displayName}
                      >
                        {rowContent}
                        <span class="sim-disclosure-chevron" aria-hidden="true">
                          <i class="sim-sf-symbol sim-chevron-right" style={symbolStyle("chevron.right")}></i>
                          <i class="sim-sf-symbol sim-chevron-down" style={symbolStyle("chevron.down")}></i>
                        </span>
                      </button>
                      {detail ? (
                        <div class="sim-feature-detail" data-sim-disclosure-detail hidden>
                          {detail.label && detail.value === undefined ? (
                            <div class="sim-detail-section-title">
                              <span data-i18n-zh>{detail.label.zh}</span>
                              <span data-i18n-en>{detail.label.en}</span>
                            </div>
                          ) : null}
                          {detail.label && detail.value !== undefined ? (
                            <>
                              <div class="sim-detail-heading">
                                <span><span data-i18n-zh>{detail.label.zh}</span><span data-i18n-en>{detail.label.en}</span></span>
                                <b data-sim-range-value>{detail.value}{detail.unit}</b>
                              </div>
                              <input
                                type="range"
                                min={detail.minimum}
                                max={detail.maximum}
                                value={detail.value}
                                data-sim-range
                                data-sim-range-unit={detail.unit}
                                aria-label={detail.label.zh}
                                data-aria-zh={detail.label.zh}
                                data-aria-en={detail.label.en}
                              />
                            </>
                          ) : null}
                          {detail.options?.length ? (
                            <div class="sim-detail-list">
                              {detail.options.map((option) => {
                                const enabled = option.enabled !== false;
                                return (
                                  <>
                                    {option.divider ? <div class="sim-detail-divider" aria-hidden="true"></div> : null}
                                    {option.mode === "switch" ? (
                                      <div class="sim-detail-list-row is-switch-row">
                                        {option.symbol ? <i class="sim-sf-symbol sim-detail-row-icon" style={symbolStyle(option.symbol)} aria-hidden="true"></i> : null}
                                        <span class="sim-detail-row-copy">
                                          <strong><span data-i18n-zh>{option.zh}</span><span data-i18n-en>{option.en}</span></strong>
                                        </span>
                                        <button
                                          class:list={["sim-toggle", { "is-on": option.selected }]}
                                          type="button"
                                          role="switch"
                                          aria-checked={option.selected ? "true" : "false"}
                                          data-sim-detail-switch
                                        ><i></i></button>
                                      </div>
                                    ) : (
                                      <button
                                        class:list={[
                                          "sim-detail-list-row",
                                          `is-${option.mode}-row`,
                                          { "is-selected": option.selected },
                                        ]}
                                        type="button"
                                        disabled={!enabled}
                                        aria-pressed={option.selected ? "true" : "false"}
                                        data-sim-detail-option
                                        data-sim-detail-mode={option.mode}
                                        data-sim-secondary-target={plugin.id === "display-resolution" && option.id === "builtin-display" ? "display-resolution" : undefined}
                                      >
                                        {option.mode === "select" ? (
                                          <span class="sim-selection-mark" aria-hidden="true">
                                            <i class="sim-sf-symbol" style={symbolStyle("checkmark")}></i>
                                          </span>
                                        ) : option.symbol ? (
                                          <i class="sim-sf-symbol sim-detail-row-icon" style={symbolStyle(option.symbol)} aria-hidden="true"></i>
                                        ) : null}
                                        <span class="sim-detail-row-copy">
                                          <strong><span data-i18n-zh>{option.zh}</span><span data-i18n-en>{option.en}</span></strong>
                                          {option.subtitle ? <small><span data-i18n-zh>{option.subtitle.zh}</span><span data-i18n-en>{option.subtitle.en}</span></small> : null}
                                        </span>
                                        {option.mode === "navigation" ? (
                                          <i class="sim-sf-symbol sim-detail-row-chevron" style={symbolStyle("chevron.right")} aria-hidden="true"></i>
                                        ) : null}
                                      </button>
                                    )}
                                  </>
                                );
                              })}
                            </div>
                          ) : null}
                        </div>
                      ) : null}
                    </>
                  ) : (
                    <div class="sim-feature-row">
                      {rowContent}
                      {type === "switch" ? (
                        <button
                          class:list={["sim-toggle", { "is-on": isEnabled }]}
                          type="button"
                          role="switch"
                          aria-checked={isEnabled ? "true" : "false"}
                          aria-label={text.zh.displayName}
                          data-aria-zh={text.zh.displayName}
                          data-aria-en={text.en.displayName}
                          data-sim-setting={plugin.id}
                        ><i></i></button>
                      ) : (
                        <button
                          class="sim-control-action"
                          type="button"
                          data-sim-action
                          data-plugin-name-zh={text.zh.displayName}
                          data-plugin-name-en={text.en.displayName}
                          data-action-zh={actionLabel.zh}
                          data-action-en={actionLabel.en}
                        >
                          <span data-i18n-zh>{actionLabel.zh}</span><span data-i18n-en>{actionLabel.en}</span>
                        </button>
                      )}
                    </div>
                  )}
                </article>
              );
            })}
          </div>
        </div>

        <div class="sim-toast" role="status" aria-live="polite" data-sim-toast></div>
      </section>

      <div
        class="sim-resolution-secondary-panel"
        role="menu"
        aria-label="内建显示器"
        data-aria-zh="内建显示器"
        data-aria-en="Built-in Display"
        data-sim-resolution-panel
        data-side="right"
        hidden
      >
        <header>
          <strong><span data-i18n-zh>内建显示器</span><span data-i18n-en>Built-in Display</span></strong>
        </header>
        <div class="sim-resolution-list">
          {simulatedDisplayResolutions.map((resolution) => (
            <button
              class:list={{ "is-selected": resolution.selected }}
              type="button"
              role="menuitemradio"
              aria-checked={resolution.selected ? "true" : "false"}
              data-sim-resolution-option
              data-sim-resolution-value={resolution.id}
            >
              <span class="sim-resolution-check" aria-hidden="true">
                <i class="sim-sf-symbol" style={symbolStyle("checkmark")}></i>
              </span>
              <span data-i18n-zh>{resolution.zh}</span>
              <span data-i18n-en>{resolution.en}</span>
            </button>
          ))}
        </div>
      </div>

      <div class="sim-calendar-popover" role="tooltip" data-sim-calendar-popover hidden>
        <div class="sim-calendar-popover-notch" aria-hidden="true"></div>
        <div class="sim-calendar-popover-heading">
          <strong data-sim-calendar-popover-title></strong>
          <small data-sim-calendar-popover-subtitle></small>
        </div>
        <div class="sim-calendar-popover-divider" aria-hidden="true"></div>
        <div class="sim-calendar-popover-events" data-sim-calendar-popover-events></div>
      </div>
    </div>
  </div>
</section>
